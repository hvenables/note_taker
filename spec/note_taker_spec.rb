# frozen_string_literal: true

RSpec.describe NoteTaker do
  it "has a version number" do
    expect(NoteTaker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
